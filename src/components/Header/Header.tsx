import React from 'react'
import { HeaderWrapper } from './Header.styles'

type Props = {}

export default function Header({}: Props) {
  return (
    <HeaderWrapper className="abc"> header</HeaderWrapper>
  )
}